# =============================================================================
# EZY Portal - Items Module Resource Limits
# =============================================================================
# High-performance overlay for the Items module.
# Applied when PERF_MODE=high is set.
# =============================================================================

services:
  items:
    deploy:
      resources:
        limits:
          memory: 2G
          cpus: '2'
        reservations:
          memory: 1G
          cpus: '1'
